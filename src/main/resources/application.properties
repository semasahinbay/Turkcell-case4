# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=b190758x
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.schema=turkcell

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=turkcell

# JWT Configuration
jwt.secret=turkcellCase4SecretKey2025SuperSecureJWTToken
jwt.expiration=900000
jwt.refresh-expiration=864000000

# Logging
logging.level.com.turkcellcase4=DEBUG
logging.level.org.springframework.security=DEBUG

# Jackson Configuration
spring.jackson.time-zone=Europe/Istanbul
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss

# Gemini AI Configuration
gemini.api.key=xxxxxxxxxxxxxxxxxxxxxxx
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent
